{
  "name": "timehelm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "npm run lint:server",
    "lint:server": "cd server && cargo clippy -- -D warnings && cargo fmt --check"
  }
}
